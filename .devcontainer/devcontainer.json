{
  "name": "demo_dev_container",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:0.203-ubuntu22.04",
  "features": {
    "python": "3.12"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y curl mysql-server && pip install --upgrade pip && pip install -r requirements.txt && curl -fsSL https://ollama.com/install.sh | sh && sudo apt-get install -y docker.io && sudo systemctl start docker && sudo systemctl enable docker",
  "customizations": {
    "vscode": {
      "extensions": [
        "cweijan.vscode-mysql-client",
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "kevinrose.vsc-python-indent",
      ]
    }
  }
}
